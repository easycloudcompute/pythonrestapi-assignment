pipeline {
	agent any
    stages {
        stage('Build on aks ') {
            steps {           
                        sh 'pwd'
                        sh 'cp -R helm/* .'
						sh 'ls -ltr'
                        sh 'pwd'
                        sh '/usr/local/bin/helm upgrade --install python-restapi-app python-restapi'
              			
            }           
        }
    }
}